pipeline {
    agent {
      label 'ansible-server'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Kien..'
                sh 'whoami;pwd'
            }
        }
    }
}
