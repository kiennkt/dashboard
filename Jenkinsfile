pipeline {
    agent {
      label 'ansible-server'
    }
    stages {
        stage('Build') {
            steps { 
                echo 'Kien Tuan Kim 123'
                sh 'whoami;pwd'
            }
        }
    }
}
