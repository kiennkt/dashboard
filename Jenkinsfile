pipeline {
    agent {
      label 'ansible-server'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Kien Tuan Kim'
                sh 'whoami;pwd'
            }
        }
    }
}
